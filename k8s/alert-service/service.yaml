apiVersion: v1
kind: Service
metadata:
  name: alert-service-service
  namespace: threat-detection
spec:
  selector:
    app: alert-service
  ports:
    - port: 8002
      targetPort: 8002
  type: LoadBalancer